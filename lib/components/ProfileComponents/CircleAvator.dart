import 'package:flutter/material.dart';

class ComtainerAvator extends StatelessWidget {
  const ComtainerAvator({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
